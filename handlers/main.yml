---
handlers:
- name: Create systemd-tmpfiles
  command: /usr/bin/systemd-tmpfiles --prefix=/sys --create

- name: Restart sshd
  systemd:
    name: sshd
    state: restarted

- name: Restart fail2ban
  systemd:
    name: fail2ban
    state: restarted
